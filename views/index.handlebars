<h1>Burgers!</h1>


<div class="left-burger">
<h2>Burgers that have been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div class="right-burger">
<h2>Burgers that have been created, but not eaten...yet.</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>
</div>


<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devour?</label><br>
    <input type="radio" name="devoured" value="1" checked> Not Eaten.<br>
    <input type="radio" name="devoured" value="0"> Already Eaten.
  </div>

  <button type="submit">Add Burger</button>
</form>

